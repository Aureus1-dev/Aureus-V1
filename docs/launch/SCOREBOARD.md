# Launch Scoreboard

Living dashboard for the First Members launch. Source of truth for scope and sequencing is `LAUNCH-001-First-Members.md`; the task breakdown is `WORKORDERS.md`. This file reflects status only — it does not decide anything.

**Last updated:** 2026-07-24

---

## Overall Progress

**48% (24 of 50 work orders complete).**

All four Pre-Gate Founder decisions (P1–P4) are complete. Gate A's schema (A1), storage (A2), candidate list (A3), and verification-workflow tooling (A4-PREP) are done — A4's human phone/contact check is still untouched. Gate B is now **fully complete** (B1–B9: V1 Scope Lockdown; arrival screen; consent captured; accessibility/communication preferences captured and applied; authenticated/unauthenticated behavior verified correct; arrival safely interruptible and resumable; steward visibility into arrival state; arrival fails safely; outcome sign-off). Gate C's engineering scope is now **fully complete** (C1–C8: Understanding; Clarification — an ambiguous need reliably gets a clarifying question, answered in the same conversation; Urgency assessment — crisis/urgent language reliably redirects to real, honest emergency help instead of an AI reply; Resource discovery — a stated need's own words are deterministically matched to City Sheet candidates; Verified resource presentation — verified/unverified/test/unavailable resources are visibly and honestly distinguished, every offer and response recorded; Steward escalation — a member can page a real human steward, with honestly published on-call hours and every escalation/outcome recorded; Safe failure — a member with no verified resource and no reachable steward is told the truth, given a real next step, and the unresolved need is recorded; and build/test sign-off — the full understanding-through-safe-failure flow proven end-to-end for one real test member against fixtures alone). C9's production-safety enforcement is also now built and merged (a real member's resource discovery and offers are proven, by test, to trace only to `VERIFIED` City Sheet data, never an unverified real candidate) — but C9 itself is not yet Done, since its acceptance criteria also requires actual real members and actual A4/A6-verified data, neither of which exists yet. Progress is calculated as completed work orders ÷ total work orders across Pre-Gate and Gates A–F (24/50).

## Overall Launch Readiness

**Not ready overall (Gate A still in progress on human verification; Gates D–F not started). Gate B is complete: 9/9 work orders done. Gate C's engineering scope is complete: 8/9 work orders done, only real-member production verification (C9) remains, blocked on Gate A.**

The Pre-Gate blocker is cleared; the city sheet's data schema (A1), storage/query layer (A2), initial candidate list (A3), and verification-workflow tooling (A4-PREP) are all done. B1 (V1 Scope Lockdown), B2 (arrival screen, three-second return, persistent Urgent-help affordance), B3 (consent and expectations captured, retrievable later), B4 (accessibility/communication preferences captured and applied — reduced motion), B5 (authenticated/unauthenticated arrival behavior verified correct, no leakage), B6 (arrival is safely interruptible and resumable), B7 (human stewards have appropriate visibility into arrival state), B8 (arrival fails safely), and B9 (Gate B outcome sign-off — the full arrival-to-handoff flow verified for a real test member under two accessibility conditions) are all done, completing Gate B in full — voice, Academy, and Pods are closed to members; a member reaches a working arrival screen with an always-available Urgent-help affordance; a member cannot proceed past arrival without granting consent, and their preferences observably change their experience from that point on. Per a Founder Decision approved 2026-07-24 (see "Governance" below), Gate B and Gate C were substantially rescoped and renamed; Gate B now owns everything from first arrival through a working "How can we help?" hand-off, and does **not** depend on Gate A — Gate B is now fully done. Gate C — The Clearing (understanding through safe failure) is now also fully built and self-tested (C1–C8): a member's need is captured, clarified when ambiguous, checked for urgency, matched against City Sheet resources, honestly labeled by verification status, escalatable to a real steward, and never left at a dead end when nothing else is available — all proven end-to-end against explicitly labeled fixtures for a real test member. **No candidate referral may be used by the Clearing in production until a Human Steward actually verifies it via the A4-PREP tooling — building and testing the workflow against fixtures is not the same as using it on real data, and this readiness note is not a claim that the city sheet is trustworthy yet; that is exactly what C9 (blocked on A4/A6) will confirm. As of this week, that claim is also now structurally enforced in code, not only in prose — an unverified real candidate cannot be matched or offered to any member, verified or not — but the claim itself ("real members, real verified data") is only actually true once A4/A5/A6 genuinely happen.** The Urgent-help affordance added in B2 deliberately shows only static, universal safety information (911, 988, Crisis Text Line) as a backstop that never depends on any of this newly built machinery.

## Governance

A Founder Decision approved 2026-07-24 reconciled a naming/scope conflict between LAUNCH-001's original Gate B ("The Clearing Drill") / Gate C ("The Spine") text and later Production Execution Order guidance that reused "Gate B" and "Gate C" for different content. The decision (now binding, recorded in full in `WORKORDERS.md`'s Revision History):

- **Gate A** is renamed "The Foundation" (content unchanged).
- **Gate B** is renamed "The Gate" — owns everything from first arrival until "How can we help?" Does not depend on Gate A.
- **Gate C** is renamed "The Clearing" — owns understanding, clarification, urgency assessment, resource discovery, verified resource presentation, steward escalation, safe failure. C1–C8 (build/fixture work) do not depend on Gate A; only C9 (real-member production verification) does.
- **Gates D–F are unchanged.**
- Work orders describe observable behavior and outcomes, never Experience Canon rooms (the Bench, Greeting, Shelf, Seal, Leaving, etc. remain Experience Canon, not engineering phases).

The old V1 Gate B/C work orders are preserved as a superseded historical record in `WORKORDERS.md`, not deleted.

## Current Gate

**Gate A — The Foundation** (in progress, human-verification-blocked) and **Gate C — The Clearing** (engineering scope fully complete, 8/9 — only C9 remains, blocked on Gate A). **Gate B — The Gate is fully complete (9/9).**

## Current Work Order

**A4 — Human-verify every referral** is now the only work remaining anywhere in Gate A or Gate C that isn't blocked — and, as of this week, its engineering side is fully closed: a real UI (`/city-sheet`, Steward/Platform Administrator only) now exists for exactly this work, replacing raw API calls as the only way to do it. A1, A2, A3, and A4-PREP (including its frontend addendum) are all done; A4's dependencies are fully satisfied — what's left is real-world phone/contact verification work for a Human Steward, not an engineering task. 8 candidates are waiting, one of which (Media Food Bank) is flagged lower-confidence and should be checked especially carefully. The full runbook is `docs/launch/A4-Verification-Guide.md`. Gate C's engineering scope (C1–C8) is now fully done; C9's own engineering safeguard (real members trace only to verified data — see "Completed This Week") is also now done. C9 itself remains Gate C's only outstanding work order, and cannot be signed off until A4 and A6 (Gate A sign-off) actually complete — engineering has nothing further to build for it.

## Current Focus

The Gate B/Gate C naming conflict previously flagged here has been resolved by Founder decision (see "Governance" above) and reflected in `WORKORDERS.md` V2. Gate B (B1–B9) is fully done. Gate C's engineering scope (C1–C8) is now fully done — the entire understanding-through-safe-failure flow has been proven end-to-end for one real test member against fixtures alone. A4's engineering side is now also fully done (the `/city-sheet` verification UI). C9's own engineering safeguard is now also fully done (see "Completed This Week") — production discovery/offers are provably restricted to verified data. The only work left anywhere in this Production Execution Order's reachable scope is now entirely human: A4 itself (a Human Steward actually phone-verifying all 8 candidates through the new UI, unblocking A5/A6) and, once A6 completes, C9's real-member sign-off (the same Clearing flow actually run with real members against real, human-verified City Sheet data instead of fixtures — the mechanism guaranteeing this is already built and merged; only the real-world event itself remains).

## Blocked Items

| Item | Blocked By | Effect |
|---|---|---|
| C9 – Gate C production verification (real members) — real-member sign-off | Gate A (A6, not complete) | Cannot start |
| Gate D (all work orders) | Gate C | Not started |
| Gate E (all work orders) | Gate C | Not started |
| Gate F (all work orders) | Gates A–E | Not started |

P1–P4, A1, A2, A4-PREP, and all of Gate B (B1–B9) no longer appear as blockers. A3 no longer blocks A4 (one of its three dependencies) — but A3's candidates themselves remain UNVERIFIED and must not be treated as trustworthy until A4 completes. Gate C's build/fixture work (C1–C8) is now fully done and was never blocked on Gate A, per the Founder's Gate B/Gate C Reconciliation decision. C9's engineering safeguard (see "Completed This Week") is also now done and merged — real-member discovery/offers already provably trace only to verified City Sheet data — but C9's own real-member sign-off remains Gate-A-blocked, listed above, since it also requires A4/A5/A6 to have actually happened, not only the engineering mechanism to exist.

## Completed This Week

- **C9 engineering safeguard — production-safety enforcement (real members trace only to verified data):** A code inspection found a genuine, already-live gap directly contradicting C9's own acceptance criteria: `NeedsService.findMatchingResources()` (C4) filtered City Sheet candidates only by `status: ACTIVE`, with no check on `verificationStatus` — meaning A3's 8 real, still-`UNVERIFIED` launch-metro candidates could already have been matched and offered to any real member today. Fixed with one rule, `isSafeToSurface()`, enforced in both `findMatchingResources` (discovery) and `offerResource` (direct offer by ID, refused with `404` even bypassing discovery): a City Sheet entry is reachable by a member only if it is explicitly `isTestFixture: true` (safe-by-construction build/test data, needed so C4–C8's own fixture tests keep working) or actually `verificationStatus: VERIFIED`. 7 new tests (6 unit, 1 e2e — the e2e test seeds one real unverified candidate and one real steward-verified candidate side by side and proves a real member's session traces only to the verified one). Full `apps/api` suite: 121/122 suites, 1261/1268 tests passing, only the same 7 pre-existing Voice Domain failures remaining; `tsc --noEmit`, `eslint --max-warnings=0`, `nest build` all clean. No frontend change. **This closes C9's engineering gap only. It was implemented under an explicit, temporary development assumption that A4/A5/A6 are complete, solely to unblock this engineering dependency — no phone verification was performed or simulated, no production verification record was created, and A4, A5, A6, and C9 are not being marked complete.** A4 (real human phone/contact verification), A5 (QA spot-check), and A6 (Founder Gate A sign-off) remain exactly as they were: real-world work only a Human Steward and the Founder can perform. Once they do, this already-merged mechanism is what makes C9's real-member claim true — no further engineering is needed.
- **A4-PREP addendum — Human Steward Verification Workflow, now reachable through the application:** a follow-up Production Execution Order scoped explicitly to A4's remaining engineering dependency ("Do not mark A4 complete... Build every engineering component required so that a Human Steward can later complete A4 through the application") closed the one real engineering gap A4-PREP left open — the workflow existed only as raw API endpoints, with "no UI built" explicitly noted at the time. New `/city-sheet` route, gated to `STEWARD`/`PLATFORM_ADMINISTRATOR` (mirroring the backend's own guard exactly), lets a Human Steward browse entries by verification status, open one to see its real checklist and call script, record confidence/checklist answers/notes, and call verify/reject/flag-for-review — every field is the steward's own unmodified input. **This performs zero verification itself and changes no backend behavior** — `apps/api/src/city-sheet/` is untouched; the new surface only relays a real human's decision to the same endpoints A4-PREP already built. 15 new tests (role-gate, state, and component/a11y coverage). Full `apps/web` suite: 107/107 suites, 573/573 tests passing (was 104/558); `tsc --noEmit`, `next lint`, `next build` (38 routes, +1 for `/city-sheet`) all clean. **A4 itself remains Not Started — real phone/contact verification of the 8 candidates is still real-world work only a Human Steward can perform; A6 and C9 remain correspondingly untouched.**
- **C8 — Gate C build/test sign-off (fixtures)**: C1–C7 each already had thorough dedicated coverage; this work order's distinct job was proving the whole understanding-through-safe-failure pipeline composes correctly as one continuous member journey against fixtures alone, never touching a `verified` City Sheet entry. New single e2e test walks one real test member through: a stated need captured from a plain message (C1); the fixture matched by category (C4); its test-fixture status honestly reported (C5); an offer made and accepted (C5); an escalation raised, acknowledged, and resolved (C6); and a final safe-failure check confirmed reachable and well-formed (C7) — all against one `POST /city-sheet` entry created with `isTestFixture: true` and never verified. C2/C3 were deliberately not re-derived in this same narrative: `POST /ai/conversations/:id/messages` carries a dedicated rate limit shared across every message-posting test in this file (PD-001), C1–C3 already exercise and pass that branch on their own, and an early draft of this test that did re-derive it tripped a real `429` — confirming the limit is live and correctly enforced, and that re-proving already-covered ground here would only spend shared budget. Zero production code changed — pure verification, exactly like B9's role for Gate B. Full `apps/api` suite: 122/122 suites, 1254/1261 tests passing, only the same 7 pre-existing Voice Domain failures remaining; `tsc --noEmit`, `eslint --max-warnings=0`, `nest build` all clean. **With C8 done, Gate C's engineering scope (C1–C8) is fully complete — only C9 remains, blocked on Gate A.**
- **C7 — Safe failure**: reuses C4's `matchCategoriesForNeed()` and C6's steward-reachability check rather than inventing new signals — a need honestly has no path forward only when its matched categories carry zero `ACTIVE`+`VERIFIED` City Sheet entries *and* zero real users hold `STEWARD`/`PLATFORM_ADMINISTRATOR`; an unmatched need (no category at all) never triggers this, since that's C4's own honest "no keyword hit," not a dead end. New singleton-per-need `UnresolvedNeed` model (migration `20260724120030_add_unresolved_needs`, unique on `statedNeedId` so a repeat check is idempotent, never a duplicate). New self-only `GET /needs/:id/safe-failure` recomputes both conditions on every call and records the outcome the moment they're both true, returning the same row on every later call while they hold. The message and next step reuse C3's `CRISIS_REDIRECT_MESSAGE`/B2's `UrgentHelpAffordance` safety numbers word-for-word (911, 988, Crisis Text Line) plus an honest statement that the need is recorded for steward follow-up — never a promise of immediate response. No new frontend surface required. 10 new tests (6 unit, 4 e2e); the e2e suite deliberately avoids hard-asserting the "no steward reachable" branch's outcome, since Steward/Admin role existence is real global database state other e2e suite files mutate concurrently in this codebase's existing parallel-worker test architecture — asserting on it would repeat the exact cross-suite fragility already found and fixed in C6's on-call-hours test. Instead it asserts what must always hold: idempotency, and that a triggered result always carries a real, non-empty message and next step. The "simulated" condition itself is exhaustively covered by the fully-mocked unit test, matching the acceptance criteria's own wording. Full `apps/api` suite: 122/122 suites, 1253/1260 tests passing, only the same 7 pre-existing Voice Domain failures remaining; `tsc --noEmit`, `eslint --max-warnings=0`, `nest build` all clean. No frontend change.
- **C6 — Steward escalation**: two new models (migration `20260724102958_add_need_escalations_and_on_call_hours`) — `NeedEscalation` (real FK-enforced row per escalation against the member and their stated need, `status: PENDING|ACKNOWLEDGED|RESOLVED`) and a singleton `PublishedOnCallHours`. Per the honest-data precedent set by A3/A4-PREP, real on-call hours aren't documented anywhere in governance and can't be fabricated: `hoursDescription` defaults to `null` and is reported that way honestly (never a placeholder like "9am–5pm") until a real Steward publishes the real rotation — the mechanism is production-ready; the one fact only a human can supply is left as explicit follow-up, exactly how A4's real verification was separated from A4-PREP's tooling. New `NeedEscalationsService` reuses `NeedsService`'s self-only ownership pattern for the member side (escalate, view own escalations) and mirrors `StewardshipEscalationsService`'s in-service role check for the steward side (acknowledge, resolve, publish hours). Paging reuses the existing Communication System exactly as built — `NotificationsService.notify()` with `bypassPreferences: true` to every Steward/Platform Administrator, so a page can never be silently suppressed by a steward's own notification preferences. Four new routes on `NeedsController` plus a new `OnCallHoursController` — no new frontend surface was required by this work order's acceptance criteria. 21 new tests (13 unit, 8 e2e: on-call hours publish/read + member-forbidden, full escalate → acknowledge → resolve lifecycle with outcome retrieval, cross-member forbidden). Full `apps/api` suite: 121/122 suites, 1243/1250 tests passing, only the same 7 pre-existing Voice Domain failures remaining; `tsc --noEmit`, `eslint --max-warnings=0`, `nest build` all clean. No frontend change.
- **C5 — Verified resource presentation**: two new facts join the resource data C4 already exposes — `CitySheetEntry.isTestFixture` (explicitly set at creation, never inferred, so build/test fixtures are honestly distinguishable from real unverified candidates) and the existing `verificationStatus`. A new `ResourceOffer` model records every offer made and the member's response (`PENDING`/`ACCEPTED`/`DECLINED`) as its own row, never silently overwritten, via three new self-only endpoints (`offer`, `respond`, list). Unlike C1–C4, this work order's acceptance criteria explicitly required visual distinguishability "in a UI review" — the plain conversation text C1–C3 relied on doesn't carry structured state, so this is Gate C's first genuinely new frontend surface: a `ResourceStatusBadge` (a pure, deterministic mapping to one of four honestly labeled states — Verified/Not yet verified/Test data/Unavailable — never defaulting to "verified"), a `MatchedResourceCard` (mirrors the existing Resource/Recommendation card pattern, adding Accept/Decline), and a `NeedResourcesPage` at a new route (`/needs/[id]`, the app's first dynamic-segment route) that shows the "Unavailable" empty state when nothing matches — the fourth resource state, not silence. 28 new tests (8 backend unit, 4 backend e2e seeding real verified + test-fixture City Sheet entries, 16 frontend across 3 component suites). Full `apps/api` suite 120/121 suites, 1225/1232 tests passing (same pre-existing Voice Domain failures only); full `apps/web` suite 104/104 suites, 558/558 tests passing (was 101/525); `next build` succeeds across 37 routes.
- **C4 — Resource discovery**: matching a stated need's own words to a City Sheet category is deterministic — a documented, per-category keyword list (`matchCategoriesForNeed()`, `apps/api/src/needs/resource-matching.util.ts`, no new schema), for the same reliability reason C2/C3 avoid model judgment. A need may match more than one category (an eviction notice is both housing and legal) or none at all (an honest "no keyword hit," not a guess); it deliberately never matches `CRISIS_LINE` or `OTHER`, since crisis routing is C3's job. New self-only `NeedsService.findMatchingResources()` queries `CitySheetService.findAll({ category, status: ACTIVE })` per matched category and dedupes, exposed via a new self-only `GET /needs/:id/resources` endpoint — the first member-reachable surface for City Sheet data, since the Gate C research pass found City Sheet had none. A new `MatchedResourceDto` deliberately excludes internal steward/audit fields (`verifiedById`, `verificationNotes`, `rejectionReason`, `sourceNotes`, `createdById`) while still carrying `verificationStatus`, the fact C5 (verified resource presentation) depends on to visibly distinguish verified from unverified. 20 new tests (13 pure-function via `it.each` plus multi-category/no-match/case-insensitivity cases, 5 service-level, 2 e2e seeding a real City Sheet entry via the steward API). No schema change.
- **C3 — Urgency assessment**: crisis/urgent language (suicide, self-harm, danger from another person) is detected deterministically — a documented, reviewable phrase list (`isCrisisLanguage()`, `apps/api/src/needs/crisis-detection.util.ts`, no new schema), for the same reliability reason as C2. Unlike C2, this check runs on *every* message, not only the first — a member may reveal urgency at any point, not just when stating their initial need — and it takes priority over C2's ambiguity check: a short crisis message (e.g. "want to die") always gets the crisis redirect, never a "tell me more" follow-up. On a match, the AI provider is never called; a fixed, honest redirect (`CRISIS_REDIRECT_MESSAGE`) is persisted directly, pointing to 911, 988, and the Crisis Text Line — copy deliberately reused word-for-word from `UrgentHelpAffordance` (B2) so crisis messaging stays consistent and never implies a capability (emergency dispatch, real-time monitoring) Aureus doesn't have. C1's capture still runs independently for a crisis-flagged first message. 17 new tests (10 pure-function via `it.each`, 4 service-level, 3 e2e). No schema or endpoint change.
- **C2 — Clarification**: an ambiguous need (`help`, `not sure`, or anything under ~15 characters) is detected deterministically — not left to the model's judgment, matching the Orchestrator's established preference for reliability over a probabilistic LLM decision, so "reliably produces a clarifying question" is literal, not probabilistic. New pure `isAmbiguousNeed()`/`CLARIFYING_QUESTION` (`apps/api/src/needs/ambiguity.util.ts`, no new schema). `ConversationsService.ask()` checks this only on the very first message (alongside C1's capture); when ambiguous, it short-circuits before any AI provider call, so no cost is incurred and the answer is guaranteed. The member's next message lands in the same conversation — the existing multi-turn history already satisfies "without restart," no new state needed. 7 new tests (3 pure-function, 3 service-level, 1 e2e covering the full ambiguous → clarify → answer → real reply round trip). No schema or endpoint change.
- **C1 — Understanding (Gate C's first work order)**: research across the existing Conversation domain, AI Orchestrator, City Sheet, and Stewardship Escalations confirmed the conversational plumbing (free text in, free text out, no menu navigation) was already fully built and reusable — the actual gap was that nothing recognized a member's own words as a stated need. New minimal `StatedNeed` model + `apps/api/src/needs/` module (`GET /needs`, self-only); `ConversationsService.ask()` now captures the very first message of a conversation as a stated need (detected via message-history length, wrapped in try/catch so a capture failure can never block the AI response itself). Deliberately does not add classification, urgency, or clarification logic — that's C2/C3, reading from this same record. 8 new tests (3 on `ConversationsService`, 2 on `NeedsService`, 3 e2e). Validation also surfaced and worked around an unrelated environmental quirk (this sandbox's real `OPENAI_API_KEY` plus blocked egress to `api.openai.com` — forcing `AI_PROVIDER=stub` for test runs restores the intended stub-provider behavior and clears it up); with that override, full `apps/api` suite: 117/118 suites, 1156/1163 tests passing, only the same pre-existing Voice Domain flakiness remaining. No schema field or endpoint outside the new module changed.
- **B9 — Gate B outcome sign-off**: every real, reachable state of the arrival-to-handoff flow (Consent through the final hand-off) had already been built and functionally tested across B1–B8, but had never once been run through an automated accessibility audit — unlike 60+ other test files across the codebase. New end-to-end test walks the full flow with `jest-axe`'s `axe()` run against the real rendered DOM at all eight states (screen-reader condition); a second new test proves the same flow is fully operable by keyboard alone, `Tab`/`Enter`, no mouse (the low-digital-confidence condition); a third extends B8's new loading/error states into the same audit. The hand-off is verified as a real, working link to `/conversation` (an already-built, already-tested route), not accepted on a route merely existing. 4 new tests, zero production-code changes — this was pure verification, and it passed. **Gate B (B1–B9) is now fully complete.**
- **B8 — Arrival fails safely**: `WelcomeFlow.tsx` only checked `isLoadingGoals`, never `journey.state.error` — a `loadGoals()` failure silently fell through to the full `FirstRunWelcome` onboarding wizard as if the member were brand-new, when in fact we simply couldn't tell whether they were returning. Fixed with an explicit, retryable `ErrorState` gate (exempting `forceNewMission`, which doesn't need that answer). This surfaced a related reducer bug: `JourneyContext` never cleared a stale `error` on a successful `'goals/loaded'` (unlike `Opportunities`/`Recommendations`, which already clear `error` at the start of every attempt) — fixed with a one-line change, otherwise a successful retry would still show the old error forever. Also added retry actions to `OpportunityDiscoveryStep` and `ReviewApprovalStep` so every arrival step that calls an API now offers a consistent, honest retry (none were ever a true dead end — `Continue` always remained reachable — but retry was inconsistently present). 6 new tests. No backend or schema change.
- **B7 — Human stewards have appropriate visibility into arrival state**: the existing steward-only `GET /stewardship/relationships/:id/member-overview` endpoint already served both audiences (a Platform Administrator already passed its `isAdmin` check), so no new endpoint or Founder-facing surface was needed. Extended `MemberOverviewResponseDto` with an `arrivalStatus` object (`consentGranted`, `consentVersion`, `consentGrantedAt`, `hasCreatedFirstGoal`), computed by injecting B3's already-exported `ConsentService` into `StewardshipRelationshipsService`. Deliberately minimal — only consent-grant metadata and whether a first Goal exists (the later wizard steps leave no separate backend signal) — nothing about a member's actual content is exposed beyond what the endpoint already returned. No new module, controller, route, or migration. 2 new unit tests, 1 new e2e test (real consent grant, verified through the steward's own view).
- **B6 — Arrival is safely interruptible and resumable**: `FirstRunWelcome`'s current step lived only in `useState` — any reload restarted the whole wizard from consent. Worse, once a member's immediate need created a Goal, `WelcomeFlow`'s returning-member check would redirect them to `/home` on reload, silently skipping onboarding they'd never actually finished. New `arrival-progress.ts` persists the current step to `localStorage`; `FirstRunWelcome` resumes from it instead of restarting, and `WelcomeFlow`'s redirect now defers to resuming when an incomplete step is persisted. `forceNewMission` clears any stale step (an intentional fresh start). Reaching the final step clears the marker. A resumed `first-mission` with no created goal falls back to `immediate-need` rather than a stuck blank screen. 5 new tests; no backend/schema change.
- **B5 — Authenticated and unauthenticated behavior is correct**: primarily verification, not new code. `AuthGate` already correctly handled every static auth state, each already covered by an existing test; the gap was that only static snapshots were tested, not the *transitions* between them. Two new dynamic tests re-render the same mounted `AuthGate` across a transition (authenticated → unauthenticated, and back) and confirm no stale protected content ever persists across the flip. Tracing `JourneyContext`'s loading-state gating (used by `WelcomeFlow`'s returning-member check) confirmed it already prevents a stale prior session's data from ever being rendered during the brief window before a freshly-authenticated member's own data loads. No bug found; no production code changed.
- **B4 — Accessibility and communication preferences are captured and applied**: `ThemeProvider` already had a fully built, already-effective reduced-motion mechanism (the `data-reduced-motion` DOM attribute, already zeroing motion-duration tokens) — the real gap was no member-facing way to set it. New `PreferencesStep` (second step of arrival, after consent) lets a member toggle "Reduce motion and animation," wired straight into that existing mechanism — no new backend, schema, or token work. A test confirms the DOM attribute actually flips on toggle and stays applied past the step that set it. Deliberately left `Profile.preferredLanguage` (already has its own Profile-page UI) and the dormant `PreferencesContext.notificationsEnabled` (would collide with the real Communication System `NotificationPreferences` domain) out of scope.
- **B3 — Consent and expectations are captured**: new append-only `ConsentRecord` model + migration, and a self-only `apps/api/src/consent/` module (`POST`/`GET /users/:userId/consent`) mirroring the `SavedOpportunities` auth pattern. `CURRENT_CONSENT_VERSION` mirrored between backend and frontend (not shared, matching B1's precedent). A new `ConsentStep` is now the first step of arrival for a genuine first-run member — states what Aureus remembers, who can see it, and how to reach real urgent help — and a member cannot advance without it; a returning member starting a new mission skips it. New `classify-arrival-error.ts` gives arrival steps that call the API directly the same error handling the Journey/Opportunities/Recommendations state modules use (reused by B4 and set up for B8).
- **B2 — First arrival is observable and safe**: the arrival screen (`/welcome` → `WelcomeFlow`) already distinguished new members (shown `FirstRunWelcome`) from returning members (redirected to `/home`); this work order closed the two real gaps. New persistent `UrgentHelpAffordance` (`apps/web/design-system/components/urgent-help/`), mounted once in the member layout so it is present on every member surface — fixed bottom-left, the one corner neither `StewardWorkspace` (bottom-right) nor `GlobalActionPalette` (top-right) occupies. It has no dependency on any API, so it cannot fail from backend downtime, and shows only static, universally true safety information (911, 988 Suicide & Crisis Lifeline, Crisis Text Line) — it honestly notes that Aureus's own local resource guide isn't verified yet, rather than implying a capability (crisis detection, resource discovery, steward paging) that doesn't exist — those remain Gate C's C1/C3/C4/C6, not yet built. Also added an automated test asserting the existing returning-member redirect fires within the 3-second budget LAUNCH-001 sets, closing a test gap (the redirect logic itself was already correct). No backend, schema, or migration change. Full `apps/web` suite 101/101 suites, 525/525 tests passing (net +1 suite, +7 tests, zero regressions); `tsc --noEmit`, `next lint`, and `next build` (all 36 routes) all clean.
- **Founder Decision — Gate B/Gate C Reconciliation:** resolved the naming and scope conflict between LAUNCH-001's original Gate B/Gate C text and the Production Execution Order's later reuse of those labels. Gate A renamed "The Foundation" (unchanged content); Gate B renamed "The Gate" and rescoped to own first arrival through "How can we help?" (not blocked on Gate A); Gate C renamed "The Clearing" and rescoped to own understanding through safe failure (build/fixture work C1–C8 not blocked on Gate A; only real-member production verification C9 is). `WORKORDERS.md` rewritten as V2 to reflect this; the superseded V1 Gate B/C content is preserved as a historical record, not deleted. Old C2 (V1 Scope Lockdown) is preserved and renumbered as new B1, still Done.
- Founder decided all four Pre-Gate items (P1–P4): launch metro (Chester and Delaware County, PA), invitation path (direct invitations first), steward staffing (Founder + one trusted human steward), and Gate F date ambition (readiness-gated, internal target 2026-08-21).
- A1 — Defined the Launch City Sheet data schema: `CitySheetEntry` model (plus `CitySheetCategory`, `LaunchAreaScope`, `CitySheetVerificationStatus`, `CitySheetEntryStatus` enums) added to `prisma/schema.prisma`, with migration `20260722140000_add_launch_city_sheet`. No organizations populated — structure only.
- A2 — Built the Launch City Sheet storage layer: `apps/api/src/city-sheet/` (repository interface + Prisma implementation, `CitySheetService`, `CitySheetController`, DTOs), wired into `app.module.ts` and the `city-sheet` Swagger tag. CRUD, archive, verify, and flag-for-review, all gated to Steward/Platform Administrator.
- A3 — Compiled and loaded the initial candidate referral list: 8 candidates (2 crisis lines, 2 food resources, 2 county assistance offices, 1 dual-county legal aid org, 1 statewide 211 line) via web research, each with a source citation, nothing fabricated. Loaded via an idempotent seed, all `verificationStatus: UNVERIFIED`, all `launchScope: CORE_LAUNCH_COUNTY`.
- A4-PREP — Built the Human Steward Verification Workflow (docs/launch/A4-Verification-Guide.md is the runbook), without performing any verification: a `REJECTED` verification status and `rejectionReason`/`verificationConfidence` fields; `sourceNotes` split out from `verificationNotes` so a steward's call notes can never overwrite the original A3 citation (a data migration moved existing citation text across automatically); a config-driven `CitySheetChecklistItem` table (20 default items seeded — 7 common + 13 category-specific — editable by Operations via API, no deploy needed); an append-only `CitySheetVerificationEvent` table so no verification event is ever lost; new `GET /city-sheet/:id/verification-guide`, `GET /city-sheet/:id/verification-history`, and `POST /city-sheet/:id/reject` endpoints; `verify`/`flag-for-review` extended to record confidence and structured checklist responses. Governance unchanged. Verified end-to-end against a live database, 30 new unit tests, full `apps/api` suite (110/112 suites pass — same 2 pre-existing Voice Domain failures, confirmed unrelated), `tsc --noEmit`, `eslint`.
- **B1 (formerly C2) — V1 Scope Lockdown**: voice, Academy, and Pods (LAUNCH-001's fully-built, explicitly-refused member domains — "No Pods, no Academy... voice entirely") are now unreachable by a V1 member, recoverable by a single flag. One central config (`apps/api/src/config/v1-feature-scope.ts`, mirrored at `apps/web/lib/config/v1-feature-scope.ts`), all three flags default off. Backend: `V1ScopeMiddleware` registered once ahead of every guard/controller, 404s `/ai/voice`, `/academy`, `/pods` for everyone while their flag is off — closes every current and future sub-controller in those trees by construction, not per-controller annotation; the AI Steward's `navigate_to_route` tool allow-list drops `academy` too, closing a client-side-state bypass a plain nav audit would have missed. Frontend: the single nav/routing-scaffold source of truth (`primarySurfaces`) filters out Academy/Pods; their routes redirect to `/home` directly, closing the stale-link/direct-route bypass; `VoiceOrchestrator` only mounts when its flag is on; `StewardWorkspace`'s voice UI is flag-gated as defense in depth. Nothing was deleted — every gated feature stays fully built and recoverable. A real bug was caught during verification: the middleware initially read `req.path`, which Express 5 resolves to `/` for a `forRoutes('*')` mount, making it a silent no-op; fixed to `req.originalUrl`, proven via a new end-to-end test against the real HTTP/guard stack. Full validation: `tsc --noEmit`/`eslint` clean both apps; `apps/api` 113/115 suites passing (same pre-existing Voice Domain/Recommendations flakiness and one pre-existing AI-summarize 503, both reproduced identically on an unmodified baseline); `apps/web` 100/100 suites, 518/518 tests passing.

## Next Recommended Task

**A4 — Human-verify every referral** (Human Stewards) is now the single remaining task standing between here and Gate A sign-off, and — once A6 follows it — Gate C's final sign-off (C9). Pull the queue (`GET /city-sheet?verificationStatus=UNVERIFIED`), fetch each candidate's `verification-guide`, make the call, record the outcome via `verify`/`reject`/`flag-for-review` with a confidence level. Start with the two crisis lines; treat "Media Food Bank" as needing extra care. No further engineering work is available to start until A4 (human-owned) progresses — Gate C's entire engineering scope (C1–C8, plus C9's own production-safety enforcement) is now done, and Gate D/E/F all depend on Gate C's full completion (C9's real-member sign-off included).

## Repository Health

No regression. C9's engineering safeguard added no migration and no frontend change: one new private method (`NeedsService.isSafeToSurface()`) enforced in two existing methods (`findMatchingResources`, `offerResource`), 6 new unit tests, 1 new e2e test. The A4-PREP frontend addendum added a new `apps/web/app/(member)/city-sheet/` route, `CitySheetVerificationGate`/`CitySheetVerificationPanel` (`apps/web/design-system/components/city-sheet/`), a new API client (`apps/web/lib/api/city-sheet.ts`), and a new state module (`apps/web/state/city-sheet/CitySheetContext.tsx`, composed into `AppStateProvider`) — `apps/api/src/city-sheet/` is completely untouched; only new frontend surface consuming already-existing endpoints. C8 added zero production code — one new e2e test in `ai.e2e.spec.ts` proving C1–C7 compose as a single flow against fixtures alone, pure verification exactly like B9's role for Gate B. C7 added one new migration (`20260724120030_add_unresolved_needs` — singleton-per-need `UnresolvedNeed`, unique on `statedNeedId`), one new pure copy file (`safe-failure.util.ts`), one new method on `NeedsService` (`checkSafeFailure`, reusing C4's category matcher and C6's steward-reachability check), and one new self-only endpoint (`GET /needs/:id/safe-failure`) — no existing endpoint's behavior changed, no frontend change. C6 added one new migration (`20260724102958_add_need_escalations_and_on_call_hours` — `NeedEscalation` and singleton `PublishedOnCallHours`), a new `NeedEscalationsService` plus two new repositories (`NeedEscalationRepository`, `OnCallHoursRepository`), four new routes on `NeedsController`, and a new `OnCallHoursController` — no existing endpoint's behavior changed, no frontend change. C5 added one new migration (`20260724100005_add_resource_offers_and_test_fixture_flag` — `CitySheetEntry.isTestFixture` + the new `ResourceOffer` model), three new self-only endpoints on `NeedsController`, and Gate C's first genuinely new frontend surface (`apps/web/design-system/components/needs/`: `ResourceStatusBadge`, `MatchedResourceCard`, `NeedResourcesPage`; new route `/needs/[id]`; new client `lib/api/needs.ts`) — no existing endpoint's or component's behavior changed. C4 added two new pure files (`resource-matching.util.ts`, `dto/matched-resource.dto.ts`), one new method each on `NeedsService`/`IStatedNeedRepository` (`findMatchingResources`/`findById`), and one new self-only endpoint (`GET /needs/:id/resources`) — the first member-reachable City Sheet surface, deliberately scoped to a member-safe DTO rather than reusing the steward-facing one. No schema change; no existing endpoint's behavior changed. C3 added one new pure-function file (`apps/api/src/needs/crisis-detection.util.ts`) and a short-circuit branch inside `ConversationsService.ask()`, ordered ahead of C2's own short-circuit — no new schema, no new endpoint. C2 added one new pure-function file (`apps/api/src/needs/ambiguity.util.ts`) and a short-circuit branch inside `ConversationsService.ask()` — no new schema, no new endpoint. C1 added one new small backend module (`apps/api/src/needs/`, new migration `20260724091538_add_stated_needs`) and a minimal hook inside `ConversationsService.ask()` — no existing endpoint's behavior changed, no existing schema field touched. B9 added 4 new tests (3 in `FirstRunWelcome.test.tsx`, 1 in `WelcomeFlow.test.tsx`) and changed zero production code — pure verification. B8 touched five existing frontend files (`WelcomeFlow.tsx`, `JourneyContext.tsx`, `OpportunityDiscoveryStep.tsx`, `ReviewApprovalStep.tsx`, `FirstRunWelcome.tsx`) — no new components, no backend/schema change. B7 extended one existing DTO (`MemberOverviewResponseDto`, new `arrivalStatus` field) and one existing service (`StewardshipRelationshipsService`, now also injects B3's `ConsentService`) — no new module, controller, route, migration, or frontend change. B6 added one new pure module (`arrival-progress.ts`, localStorage read/write/clear) and small, targeted changes to `FirstRunWelcome.tsx`/`WelcomeFlow.tsx` — no backend/schema change. B5 added two new dynamic tests to `AuthGate.test.tsx` with zero production-code change. B3 added a new backend module (`apps/api/src/consent/`, new migration `20260724061755_add_consent_records`) and a new frontend arrival step (`ConsentStep`); B4 added one new frontend arrival step (`PreferencesStep`) wired to an existing, already-tested mechanism (`ThemeProvider`'s motion preference) with no backend/schema change. `tsc --noEmit` and `eslint` clean for `apps/api`; `nest build` succeeds. Full `apps/api` Jest suite (against a live database, `AI_PROVIDER=stub` set explicitly — see C1's implementation note for why): 120/121 suites, 1225/1232 tests passing, with the only remaining failures being the same pre-existing Voice Domain flakiness noted in every prior work order's validation — the new/updated `needs`/`conversations`/`city-sheet` suites pass cleanly. `apps/web` (touched again by C5, first time since B9): `tsc --noEmit`, `next lint`, and `next build` (37 routes, +1 for `/needs/[id]`) all clean; full Jest suite 104/104 suites, 558/558 tests passing (542 before C5, +16 — zero regressions). `apps/web` was unchanged by B7, C1–C4. B2 (prior work order) added one new frontend component directory (`UrgentHelpAffordance`) with no backend/schema change; B1 (prior, under its earlier "C2" label) added one new backend middleware + config module — both previously validated clean and unaffected by this week's changes.

---

## Gates A–F

| Gate | Title | Status | Progress | Owner | Dependencies |
|---|---|---|---|---|---|
| A | The Foundation | **In Progress** | 57% (4/7 work orders) | Stewards + Founder | P1 (satisfied) |
| B | The Gate | **Done** | 100% (9/9 work orders) | Engineering | None — complete |
| C | The Clearing | **In Progress** | 89% (8/9 work orders) | Engineering | Gate B (B9) functional; C1–C8 not blocked by Gate A (all now Done), C9 requires Gate A |
| D | The Tending Run | Not Started | 0% (0/7 work orders) | Engineering + Stewards | Gate C |
| E | Memory rights live | Not Started | 0% (0/6 work orders) | Engineering | Gate C |
| F | The Founding Review | Not Started | 0% (0/8 work orders) | Founder | Gates A–E, P2 (satisfied), P4 (satisfied, target 2026-08-21) |

Nothing in Gates A–F may begin ahead of its listed dependency, per LAUNCH-001's execution order ("each blocking the next") and WORKORDERS.md's per-item Dependencies column, except where a Founder decision has explicitly authorized parallel work: Gate B (B1–B9, proceeded in parallel with A4, now fully complete), and Gate C's fixture-based work (C1–C8), per the Gate B/Gate C Reconciliation decision.
