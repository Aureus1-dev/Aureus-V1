/**
 * Version identifier for the current arrival consent + "what to expect"
 * copy (Gate B — B3). Must match `apps/api/src/consent/consent.constants.ts`
 * exactly. Mirrored (not shared) — the two apps intentionally do not share
 * a package for one string, matching the V1 feature-scope precedent (B1).
 */
export const CURRENT_CONSENT_VERSION = 'v1-2026-07';
